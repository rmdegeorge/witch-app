import '@testing-library/jest-dom';
import FormulaCalculator from './FormulaCalculator';
import {render} from '@testing-library/react';
/**
 * Inputs: herb, percent of total, total volume
 * Outputs: volume of each herb, percent remaining
 */

describe('Formula Calculator page', () => {
  it('should ', () => {

  });
});